console.log("T03 - Ejercicio 21");

/**
 * Busca en internet dos expresiones regulares. Una que permita validar un DNI y otra que permita validar un CIF. Crea script que pida al usuario una cadena y determine si es DNI o CIF válido. Es necesario que definas una función que se denomine "validaDNIyCIF()" que reciba una cadena y devuelva un booleano.

La expresión regular debes crear usando el método: 


Después, busca el algoritmo que permite comprobar si el DNI o el CIF son correctos (es decir, si corresponde la letra al número)

Puedes usar una IA para generar el patrón y el segundo algoritmo de comprobación, entiendo el código dado.

 */